/**
 * Adds one the the input
 *
 * @func
 * @param {Number} x the number to be incremented by 1
 * @returns {Number}
 *
 * @example
 * const number = 1
 * const numberPlusOne = addOne(number)
 * assert(number + 1 === numberPlusOne)
 */
const addOne = x => x + 1

export default addOne

